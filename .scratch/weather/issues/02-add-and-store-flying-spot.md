# 02 — Add and store a FlyingSpot

**What to build:** A pilot can add a named FlyingSpot with coordinates, see it in a saved list, and delete it. Pure CRUD — no weather fetch yet.

**Blocked by:** 01 — App shell + DroneProfile creation & storage (needs the local persistence utility and app shell it introduces)

**Status:** ready-for-agent

- [ ] Pilot can add a FlyingSpot with a name and coordinates
- [ ] Saved FlyingSpots appear in a list view
- [ ] Pilot can delete a saved FlyingSpot
- [ ] FlyingSpots persist locally across app restarts
